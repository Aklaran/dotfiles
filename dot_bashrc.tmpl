# Keep bash as a thin compatibility shim and hand off interactive shells to zsh.
if [[ $- == *i* ]] && command -v zsh >/dev/null 2>&1 && [[ -z "${ZSH_VERSION:-}" ]]; then
  export SHELL="$(command -v zsh)"
  exec zsh -l
fi
